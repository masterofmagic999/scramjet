# Port the dev server listens on (default: 1337)
PORT=1337

# AES-256-GCM key for the server-side cookie store (file-based mode only).
# Set any non-empty string; the actual key is derived with scrypt internally.
# Leave unset to disable encryption (cookies stored as plain JSON).
# Not used when Supabase is configured.
STORE_KEY=

# Optional: path to the cookie store JSON file (default: .cookies.json)
# Only used when Supabase is NOT configured.
# COOKIE_STORE_PATH=.cookies.json

# ── Supabase backend (optional but recommended for persistent sessions) ───────
# When SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are set, cookies are stored
# in Supabase instead of a local JSON file.  This means:
#   • Sessions survive Codespace restarts automatically.
#   • Multiple users each get their own isolated cookie store.
#   • Memory usage stays flat – no cookie data is held in the Node.js heap.
#
# Setup (one-time):
#   1. Create a free project at https://supabase.com
#   2. In the Supabase SQL editor run:
#        CREATE TABLE IF NOT EXISTS cookie_store (
#          user_id    TEXT PRIMARY KEY,
#          cookies    JSONB NOT NULL DEFAULT '{}',
#          updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
#        );
#   3. Copy your project URL and keys from Settings → API.
#
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Limit V8 heap size to reduce memory usage in low-RAM Codespace tiers.
# The devcontainer.json already sets this automatically.
# NODE_OPTIONS=--max-old-space-size=512
